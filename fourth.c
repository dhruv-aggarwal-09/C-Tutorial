#include <stdio.h>

int main(){

// 07-02-2024
// 1. WAP to input two numbers and find sum.
    // int a=20;
    // int b=25;

    // printf("The value of a+b is: %d",a+b);

// 2. WAP to input two numbers and find multiplication.
    // int a=29;
    // int b=30;

    // printf("The multiplication value of a*b is:%d\n", a*b);


// 3. WAP to input number and calculate its square and cube.
    // int number;

    // printf("Enter the number\n");
    // scanf("%d", &number);
    // printf("Square of input number is: %d\n", number*number);
    // printf("Cube of input number is: %d", number*number*number);


// 4. WAP to input length and breadth of a rectangle and calculate its area. Area=Length*Breadth.
    // int length, breadth;

    // printf("Enter the length:\n");
    // scanf("%d", &length);
    // printf("Enter the breadth:\n");
    // scanf("%d", &breadth);

    // printf("Area of rectangle:%d", length*breadth);


// 5. WAP to input radius and calculate area of circle and circumference of a circle. Area of circle = 3.14*r*r, Circumference of circle = 2*3.14*r
    // float radius;

    // printf("Enter the radius:\n");
    // scanf("%f", &radius);

    // printf("Area of circle: %.2f\n", 3.14*radius*radius);
    // printf("Circumference of circle: %.2f\n", 2*3.14*radius);


// 6. WAP to input radius and height of a cylinder and calculate volume of cylinder. Volume=3.14*r*r*h
    // float radius;
    // int height;

    // printf("Enter the radius: ");
    // scanf("%f", &radius);
    // printf("Enter the height: ");
    // scanf("%d", &height);

    // printf("Volume of cylinder is: %.2f", 3.14*radius*radius*height);



    return 0;
}