#include <stdio.h>

int main(){
    // Q1
    // Using hard coded inputs
    // int l = 10;
    // int w = 2.9;
    // int area = l * w;
    // printf("The area of rectangle is %d\n",area);

    // // Using inputs supplied by the user
    // int length, breadth;
    // printf("Enter the length: ");
    // scanf("%d", &length);
    // printf("Enter the width: ");
    // scanf("%d", &breadth);
    // area  = length * breadth;
    // printf("The area of rectangle is: %d\n", area);


    // Q2
    // int radius = 9;
    // float pi = 3.14;
    
    // int area = pi*radius*radius;
    // printf("The area of circle is %d\n", area);

    // int height = 6;
    // printf("The volume of cylinder is %d", area*height);

    //Q3
    // float c = 37;
    // float far = (c * 9 / 5) + 32;

    // printf("The converted value is %f", far);


    // Q4
    int p=1000;
    int years=3;
    int rate=5;

    printf("The Simple Interest for 3 years is: %d", (p*years*rate)/100);

    return 0;
}


